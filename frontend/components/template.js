/*import React from "react";
import { icon } from "@iconscout/react-unicons";

export default function (icon, text) {
    const IconButton = ({ icon, text }) => {
        return (
            
            <button
                className="flex items-center justify-center gap-2 px-4 py-2 rounded-lg bg-yellow-500 text-white"
            >
                {icon && React.createElement(icon)}
                {text && <span>{text}</span>}
            </button>
        );
    };
}*/